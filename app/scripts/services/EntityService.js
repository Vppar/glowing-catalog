(function(angular) {
    'use strict';

    angular.module('tnt.catalog.entity.service', []).service('EntityService', function EntityService() {
        this.find = function(id) {
            // return the entity if found or null otherwise
        };
    });
})(angular);
