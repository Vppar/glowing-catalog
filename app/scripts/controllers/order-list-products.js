(function(angular) {
    'use strict';
    angular.module('tnt.catalog.orderList.products.ctrl', [
        'tnt.catalog.order.service', 'tnt.utils.array'
    ]).controller('OrderListProductsCtrl', function($scope, $location, $filter, OrderService, ArrayUtils, DataProvider) {

    });
}(angular));